# Núcleo de la aplicación
PySide6>=6.6            # GUI + QtWebEngine (navegador embebido para interceptar nxm://)
requests>=2.28         # Cliente HTTP para la API de Nexus y descargas

# Extracción de archivos (opcionales pero recomendados)
py7zr>=0.20            # Archivos .7z (muy comunes en Nexus)
rarfile>=4.0          # Archivos .rar  (requiere 7-Zip o WinRAR/unrar en el sistema)
